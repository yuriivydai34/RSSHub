import type { Namespace } from '@/types';

export const namespace: Namespace = {
    name: 'Generic Sitemap News',
    url: 'sitemap-news',
    description: 'Parses a plain (non-Google-News-extension) sitemap.xml, filters for news/press-release URLs, and fetches each page for a title.',
    lang: 'en',
};
